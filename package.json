{
  "name": "check",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --no-cors",

    "start": "json-server --watch db.json --no-cors",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ngrok": "ngrok http 3000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  },
  "devDependencies": {},
  "description": ""
}
